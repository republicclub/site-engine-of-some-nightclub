debug: true
club:
  site:
    domain: http://localhost
spring:
  datasource:
    database: afisha
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:${spring.datasource.database};DB_CLOSE_DELAY=-1;MODE=MySQL;TRACE_LEVEL_FILE=4
    username: sa
    password: sa